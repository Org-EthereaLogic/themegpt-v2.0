# ThemeGPT ADWS

AWS isolated workflow automation layer for ThemeGPT v2.0.

This package contains:
- ADWS core modules (`adw_modules/`)
- Phase execution scripts (`scripts/`)
- Trinity prompts (`prompts/`)
- Test suite (`tests/`)

Use `uv run pytest` from this directory to run tests.
